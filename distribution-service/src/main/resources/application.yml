server:
    port: 8083

spring:
    application:
        name: DISTRIBUTION-SERVICE

eureka:
    client:
        register-with-eureka: true
        fetch-registry: true
        service-url:
            defaultZone: http://service-registry:8085/eureka
        instance:
            hostname: localhost

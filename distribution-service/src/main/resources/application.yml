server:
    port: 8083

spring:
    application:
        name: DISTRIBUTION-SERVICE
    datasource:
        url: jdbc:postgresql://localhost:5434/ReferYou
        platform: POSTGRESQL
        username: backend
        password: PASSWORD

eureka:
    client:
        register-with-eureka: true
        fetch-registry: true
        service-url:
            defaultZone: http://service-registry:8085/eureka
        instance:
            hostname: localhost

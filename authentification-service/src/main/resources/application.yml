server:
    port: 8084

spring:
    application:
        name: AUTHENTIFICATION-SERVICE

eureka:
    client:
        register-with-eureka: true
        fetch-registry: true
        service-url:
            defaultZone: http://localhost:8085/eureka
        instance:
            hostname: localhost

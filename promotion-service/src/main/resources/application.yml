server:
    port: 8081

spring:
    application:
        name: PROMOTION-SERVICE

eureka:
    client:
        register-with-eureka: true
        fetch-registry: true
        service-url:
            defaultZone: http://localhost:8085/eureka/
        instance:
            hostname: localhost
